// Rotating icons logic (no speed control anymore)
const rotatingIcons = document.querySelectorAll('.rotating-icon');

// No longer need speed control buttons, so we just leave the rotation animation as is
